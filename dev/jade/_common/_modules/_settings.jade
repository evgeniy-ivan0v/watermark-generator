.settings
	h2.settings__main-title(data-localize="settings")= local.rus.settings
	.settings__form-holder
		form(method="post" action="backend/download.php" enctype="multipart/form-data").settings__form
			.settings__form-row
				include _upload
			.settings__form-row
				include _position
			.settings__form-row
				include _opacity
			.settings__form-row
				include _button
